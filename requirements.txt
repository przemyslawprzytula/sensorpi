# Greenhouse Sensor System Requirements
#
# This project has a split architecture:
# - RPi: Sensor collector (minimal deps) -> use requirements-rpi.txt
# - Server: Web interface (full stack) -> use requirements-server.txt
#
# For development on a full machine, install both:
#   pip install -r requirements.txt

# Include RPi requirements (minus hardware-specific libs for dev)
# Note: RPi.GPIO and adafruit libs only work on Raspberry Pi
smbus2>=0.4.2

# Web Framework
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
Flask>=3.0.0
Flask-SocketIO>=5.3.0

# Database
SQLAlchemy>=2.0.0
PyMySQL>=1.0.2

# Utilities
APScheduler>=3.10.0
python-dotenv>=1.0.0
requests>=2.28.0
tenacity>=8.2.0
loguru>=0.7.2
python-dateutil>=2.9.0

# Testing
pytest>=7.0.0
